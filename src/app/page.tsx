import { Header, Main } from "@/app/_widgets";

const Home = () => {
  return (
    <div className="min-h-screen flex flex-col overflow-x-hidden">
      <Header />
      <Main />
    </div>
  );
};

export default Home;
