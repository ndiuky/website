import { Header, Main } from "@/app/_widgets";

const Home = () => {
  return (
    <div className="flex flex-col overflow-x-hidden">
      <Header />
      <Main />
    </div>
  );
};

export default Home;
